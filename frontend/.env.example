# Frontend environment variables

# Base URL of the CodexUg backend API (no trailing slash)
# Used by RelatedArticles and ContactForm to reach the API
# In development, the backend runs on port 5000 by default
NEXT_PUBLIC_API_URL=http://localhost:5000
